# APT Model 自动化测试工具 - 使用指南

## 📦 已创建的文件

我为你创建了以下自动化测试工具：

1. **test_all_commands.py** - 自动化测试脚本（主程序）
2. **view_test_report.py** - 友好的报告查看器
3. **quick_test.sh** - 一键运行测试和查看报告
4. **README_TEST.md** - 详细的英文说明文档
5. **本文档** - 中文快速指南

## 🚀 快速开始

### 方法一：一键运行（推荐）

```bash
bash quick_test.sh
```

这个脚本会：
1. 自动运行所有命令测试
2. 生成详细的日志文件
3. 显示友好的报告

### 方法二：分步运行

```bash
# 1. 运行测试
python test_all_commands.py

# 2. 查看报告
python view_test_report.py
```

## 📊 测试结果

测试完成后，会在 `test_logs/` 目录生成两个文件：

```
test_logs/
├── command_test_YYYYMMDD_HHMMSS.log   # 详细文本日志
└── command_test_YYYYMMDD_HHMMSS.json  # 结构化 JSON 数据
```

## 📝 查看报告

### 查看最新报告
```bash
python view_test_report.py
```

### 查看指定报告
```bash
python view_test_report.py test_logs/command_test_20251216_080749.json
```

## 🎯 测试了什么？

### 自动测试的命令（共32个）

**核心命令（18个）：**
- 训练相关: train, train-custom, fine-tune, train-hf, train-reasoning, distill
- 交互: chat
- 评估: evaluate, visualize, compare, test
- 工具: clean-cache, estimate, process-data
- 信息: info, list, size
- 维护: prune, backup
- 分发: upload, export-ollama
- 通用: help

**Console 命令（14个）：**
- console-status, console-help, console-commands
- modules-list, modules-status, modules-enable, modules-disable, modules-reload
- debug, config

### 测试策略

✅ **完整测试** - 大部分命令
⊘ **跳过** - 长时间运行的训练命令（train, train-custom 等）
🔍 **帮助测试** - 部分命令只测试 `--help` 参数

## 📤 发送给开发者

当你运行测试后，只需要将 `test_logs/` 目录打包发送：

```bash
# 方法一：打包整个目录
tar -czf test_logs.tar.gz test_logs/

# 方法二：只发送最新的日志
# 直接发送 test_logs/ 目录中最新的两个文件（.log 和 .json）
```

## 🔧 当前测试结果

从刚才的测试来看，主要问题是：

### ❌ 核心问题
```
ModuleNotFoundError: No module named 'torch'
```

**所有25个失败的命令都是因为缺少 torch 依赖。**

### 💡 修复建议

1. **安装 PyTorch 和核心依赖：**
   ```bash
   pip install torch transformers
   ```

2. **或安装完整依赖：**
   ```bash
   pip install -r requirements.txt
   ```

3. **重新运行测试：**
   ```bash
   bash quick_test.sh
   ```

## 📋 报告示例

运行 `python view_test_report.py` 会显示：

```
================================================================================
APT Model 命令测试报告
================================================================================

📊 测试摘要
   时间: 2025-12-16T08:08:04.076663
   总计: 32 个命令
   ✓ 通过: 0
   ✗ 失败: 25
   ⊘ 跳过: 7
   成功率: 0.0%

--------------------------------------------------------------------------------
📋 详细结果

❌ 失败的命令:
   • evaluate
     错误: None
     退出码: 1
     详情: Traceback...

⊘ 跳过的命令:
   • train: Interactive or long-running
   • chat: Interactive or long-running

--------------------------------------------------------------------------------
🔍 根本原因分析

   缺失依赖:
      • torch

--------------------------------------------------------------------------------
💡 修复建议

   1. 安装 PyTorch 和相关依赖:
      pip install torch transformers

   2. 安装完整依赖:
      pip install -r requirements.txt
```

## ⚙️ 自定义配置

如果需要修改测试行为，编辑 `test_all_commands.py`：

```python
# 跳过特定命令
SKIP_COMMANDS = {
    "chat",
    "train",
}

# 为命令添加参数
COMMAND_ARGS = {
    "console-help": ["train"],
}

# 只测试 --help
HELP_ONLY_COMMANDS = {
    "evaluate",
}
```

## 🐛 故障排查

### 问题：测试脚本无法运行
**解决方案：**
```bash
# 检查 Python 版本（需要 3.8+）
python --version

# 赋予执行权限
chmod +x test_all_commands.py view_test_report.py quick_test.sh
```

### 问题：找不到测试报告
**解决方案：**
```bash
# 确保先运行了测试
python test_all_commands.py

# 检查 test_logs 目录
ls -la test_logs/
```

## 📞 获取帮助

如果有任何问题：

1. 查看详细日志：`test_logs/command_test_*.log`
2. 查看 JSON 数据：`test_logs/command_test_*.json`
3. 将日志发送给开发者一次性修复所有问题

## 🎉 下一步

1. ✅ 已创建自动化测试工具
2. ⏳ 等待你安装依赖
3. 🔄 安装后重新运行测试
4. 📤 将结果发给开发者进行修复

---

**提示：** 这个工具会持续更新，随着项目的发展自动发现新的命令！
