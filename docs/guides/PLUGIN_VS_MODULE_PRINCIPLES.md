# 插件 vs 模块 设计原则

**Version**: 1.0
**Last Updated**: 2026-01-22

---

## 🎯 核心原则

**不是所有功能都该做成插件！**

插件化是为了提升架构灵活性，但过度插件化会导致复杂度增加和性能下降。

---

## ✅ 应该做成插件的

### 1. 可选的增强功能
**特征**: 禁用后不影响核心功能

**示例**:
- ✅ `monitoring/gradient_monitor` - 梯度监控（调试用）
- ✅ `monitoring/resource_monitor` - 资源监控（观测性）
- ✅ `visualization/model_visualization` - 训练可视化
- ✅ `evaluation/model_evaluator` - 模型评估

**判断标准**:
- 问: "如果移除这个功能，核心训练流程还能跑吗？"
- 答: "能" → 插件候选

---

### 2. 外部服务集成
**特征**: 与第三方服务/协议的集成

**示例**:
- ✅ `integration/web_search` - Web搜索集成
- ✅ `integration/graph_rag` - GraphRAG外部系统
- ✅ `integration/ollama_export` - Ollama导出
- ✅ `protocol/mcp_integration` - MCP协议

**判断标准**:
- 问: "这个功能依赖外部服务吗？"
- 答: "是" → 插件候选

---

### 3. 可选的训练方法
**特征**: 替代性的训练/优化算法

**示例**:
- ✅ `rl/rlhf_trainer` - RLHF对齐训练
- ✅ `rl/dpo_trainer` - DPO对齐训练
- ✅ `rl/grpo_trainer` - GRPO对齐训练
- ✅ `optimization/mxfp4_quantization` - 特定量化方法

**判断标准**:
- 问: "用户可以选择不使用这个训练方法吗？"
- 答: "可以" → 插件候选

---

### 4. 实验性功能
**特征**: Beta功能、研究特性

**示例**:
- ✅ `experimental/multimodal_training` - 多模态训练
- ✅ `experimental/advanced_debugging` - 高级调试
- ✅ `experimental/virtual_blackwell` - GPU仿真（未来）

**判断标准**:
- 问: "这个功能还在实验阶段吗？"
- 答: "是" → 插件候选

---

## ❌ 应该保持为模块/工具的

### 1. 核心工具
**特征**: 系统级工具，不是运行时功能

**示例**:
- ❌ `tools/apx/converter` - **APX打包工具**（工具，不是插件）
- ❌ `tools/data_generation/*` - 数据生成工具
- ❌ `tools/diagnostics/*` - 诊断工具

**为什么不是插件**:
- APX是**打包工具**，用于部署前的模型打包
- 不是运行时加载的功能
- 做成插件反而增加复杂度

**正确位置**: `tools/` 或 `scripts/`

---

### 2. 核心数据处理
**特征**: 训练流程必需的数据处理

**示例**:
- ❌ `apt/core/data/data_processor` - **核心数据处理**
- ❌ `apt/core/data/pipeline` - **数据管道**
- ❌ `apt/core/data/dataset` - 数据集抽象

**为什么不是插件**:
- 这些是训练的核心组件
- 数据处理是必需流程，不是可选增强
- 位于 `apt/core/` 层，是L0核心功能

**正确位置**: `apt/core/data/`

---

### 3. 基础设施模块
**特征**: 跨层的基础设施，系统级组件

**示例**:
- ❌ `apt/core/config` - 配置管理
- ❌ `apt/core/registry` - 注册中心
- ⚠️ `apt/perf/infrastructure/logging` - **已转为插件，需重新评估**

**需要讨论**:
- Logging是否应该是插件？
- 如果是必需的基础设施，应该保持在 `apt/perf/infrastructure/`
- 如果是可选的增强日志，可以是插件

---

### 4. 核心算法实现
**特征**: APT的核心技术

**示例**:
- ❌ `apt/core/modeling/apt_model` - APT核心算法
- ❌ `apt/core/training/trainer` - 训练器
- ❌ `apt/core/generation/generator` - 生成器

**为什么不是插件**:
- 这些是APT的定义性功能
- 移除后APT就不是APT了

**正确位置**: `apt/core/`

---

## 🤔 边界案例判断

### 案例 1: HuggingFace Loader
**当前位置**: `apt/core/data/huggingface_loader.py`

**分析**:
- ✅ 外部服务集成（HuggingFace Hub）
- ✅ 可选功能（用户可以用本地数据）
- ❌ 但数据加载是常见需求

**建议**:
- **保持为模块**，但放在 `apt/apps/` 而不是 `apt/core/`
- 或者做成 `integration/huggingface_plugin`

**决策依据**:
- 如果大多数用户需要 → 模块
- 如果只有部分用户需要 → 插件

---

### 案例 2: Chat Interface
**当前位置**: `apt_model/interactive/chat.py`

**分析**:
- ✅ 可选的UI层
- ✅ 不是核心训练功能
- ✅ 可以禁用

**建议**: `integration/chat_plugin`

---

### 案例 3: Unified Evaluation
**当前位置**: `apt/apps/evaluation/unified.py`

**分析**:
- ⚠️ 评估框架抽象
- ⚠️ 可能被多个评估插件依赖

**建议**:
- 如果是基础框架 → 保持为 `apt/apps/evaluation/base.py`
- 如果是特定实现 → `evaluation/unified_eval_plugin`

---

## 📋 决策流程图

```
功能X
  │
  ├─ 是核心算法/训练流程？
  │   └─ 是 → ❌ 保持为模块 (apt/core/)
  │
  ├─ 是工具/脚本？
  │   └─ 是 → ❌ 保持为工具 (tools/)
  │
  ├─ 是基础设施？
  │   ├─ 必需 → ❌ 保持为模块 (apt/perf/infrastructure/)
  │   └─ 可选 → ✅ 可以做插件 (plugins/infrastructure/)
  │
  ├─ 是外部服务集成？
  │   └─ 是 → ✅ 做插件 (plugins/integration/)
  │
  ├─ 是可选训练方法？
  │   └─ 是 → ✅ 做插件 (plugins/rl/ 或 plugins/optimization/)
  │
  ├─ 是实验功能？
  │   └─ 是 → ✅ 做插件 (plugins/experimental/)
  │
  └─ 是可选增强？
      └─ 是 → ✅ 做插件 (plugins/monitoring/, plugins/evaluation/, etc.)
```

---

## 📊 当前状态审查

### ✅ 正确的插件 (17个)

| Plugin | Category | 正确性 |
|--------|----------|--------|
| gradient_monitor | monitoring | ✅ 正确 |
| resource_monitor | monitoring | ✅ 正确 |
| model_visualization | visualization | ✅ 正确 |
| model_evaluator | evaluation | ✅ 正确 |
| model_comparison | evaluation | ✅ 正确 |
| logging | infrastructure | ⚠️ 需评估 |
| compression | core | ✅ 正确 |
| training_monitor | core | ✅ 正确 |
| version_manager | core | ✅ 正确 |
| graph_rag | integration | ✅ 正确 |
| ollama_export | integration | ✅ 正确 |
| web_search | integration | ✅ 正确 |
| teacher_api | distillation | ✅ 正确 |
| visual_distillation | distillation | ✅ 正确 |
| multimodal_training | experimental | ✅ 正确 |
| data_processors | experimental | ✅ 正确 |
| advanced_debugging | experimental | ✅ 正确 |

### ⚠️ 需要评估

1. **logging_plugin** - 是否应该保持在 `apt/perf/infrastructure/`？

### ❌ 已移除（Tier 2修正）

- ~~APX Converter~~ - 保持为工具
- ~~Data Processor~~ - 保持为核心模块
- ~~Pipeline~~ - 保持为核心模块

---

## 🎯 修正后的 Tier 2 计划

### 真正应该转换的 (8个)

1. **optimization/** (1)
   - mxfp4_quantization - 可选优化技术

2. **rl/** (4)
   - rlhf_trainer - 可选对齐方法
   - dpo_trainer - 可选对齐方法
   - grpo_trainer - 可选对齐方法
   - reward_model - RL工具

3. **protocol/** (1)
   - mcp_integration - 外部协议

4. **retrieval/** (2)
   - rag_integration - 可选RAG
   - kg_rag_integration - 可选KG+RAG

**总计**: 8个模块（从10个减少）

---

## 💡 最佳实践

### DO ✅
- 将可选功能做成插件
- 将外部集成做成插件
- 将实验功能做成插件
- 保持核心功能为模块
- 保持工具为工具

### DON'T ❌
- 不要把所有东西都做成插件
- 不要把核心算法做成插件
- 不要把打包工具做成插件
- 不要把必需的数据处理做成插件
- 不要为了插件化而插件化

---

## 🔗 相关文档

- `PLUGIN_CATALOG.md` - 当前插件目录
- `PLUGIN_CONVERSION_ROADMAP.md` - 转换计划
- `docs/guides/COMPLETE_TECH_SUMMARY.md` - 架构文档

---

**总结**: 插件化是手段，不是目的。正确区分插件vs模块，才能构建清晰、灵活、高性能的架构。
