# APT-Transformer 目录清理计划

## 当前问题分析

### 🚨 严重问题

1. **根目录混乱** - 有很多不应该在根目录的文件夹
2. **重复目录** - apt/ 和 apt_model/ 功能重叠
3. **apt_model/下有23个子目录** - 大部分需要迁移或清理
4. **临时文件散落** - 各种测试文件、配置文件在根目录

---

## 根目录问题

### ❌ 不应该存在的目录

| 目录 | 问题 | 建议处理 |
|------|------|----------|
| `bert/` | BERT模型不应在根目录 | → 移动到 `resources/models/bert/` 或删除 |
| `training/` | 25个训练脚本散落 | → 移动到 `examples/training_scripts/` |
| `data/` | 数据集在根目录 | → 保留但重命名为 `datasets/` |
| `demo_visualization/` | 演示文件 | → 移动到 `examples/visualization/` |
| `profiles/` | 配置文件 | → 移动到 `examples/profiles/` |
| `archived/` | 归档文件 | ✓ 保留 (已有良好结构) |
| `experiments/` | 实验配置 | ✓ 保留 (可能在使用) |

### ❌ 不应该存在的文件

| 文件 | 问题 | 建议处理 |
|------|------|----------|
| `fix_torch_imports.py` | 临时脚本 | → 移动到 `scripts/maintenance/` 或删除 |
| `fix_issues.sh` | 自动生成的脚本 | → 删除 (可重新生成) |
| `make_repo_index.py` | 工具脚本 | → 移动到 `tools/` |
| `repo_index.json` | 生成的文件 | → 删除 (可重新生成) |
| `train.py` (软链接) | 指向 training/train.py | → 删除 (已有CLI) |

---

## apt_model/ 目录问题

### 当前 apt_model/ 子目录 (23个)

```
apt_model/
├── agent/              ❌ 需要迁移到 apt/apps/agent/
├── api/                ❌ 需要迁移到 apt/apps/api/
├── cli/                ❌ 需要迁移到 apt/apps/cli/
├── codecs/             ⚠️  检查后决定 (可能保留作为工具)
├── config/             ❌ 需要迁移到 apt/core/config/
├── console/            ❌ 需要迁移到 apt/apps/console/
├── core/               ❌ 需要迁移到 apt/core/
├── data/               ❌ 需要迁移到 apt/core/data/
├── evaluation/         ❌ 需要迁移到 apt/apps/evaluation/
├── generation/         ❌ 需要迁移到 apt/core/generation/
├── infrastructure/     ⚠️  检查内容后决定
├── interactive/        ❌ 需要迁移到 apt/apps/interactive/
├── memory/             ❌ 需要迁移到 apt/memory/
├── modeling/           ✅ 已经是代理，保留向后兼容
├── optimization/       ⚠️  检查是否应该在 apt/perf/
├── pretraining/        ❌ 需要迁移到 apt/core/pretraining/
├── report/             ❌ 需要迁移到 apt/apps/report/
├── resources/          ⚠️  检查内容 (可能是静态资源)
├── rl/                 ❌ 需要迁移到 apt/apps/rl/
├── runtime/            ❌ 需要迁移到 apt/core/runtime/
├── tools/              ❌ 需要迁移到 apt/apps/tools/
├── training/           ✅ 已经是代理，保留向后兼容
├── utils/              ✅ 保留 (工具函数库)
└── webui/              ❌ 需要迁移到 apt/apps/webui/
```

### apt_model/ 根目录的杂乱文件

```
apt_model/
├── apt_optuna_20250310_1602.db              ❌ 数据库文件，应删除
├── best_train_cmd_20250310_162705.bat       ❌ 生成的脚本，应删除
├── best_train_cmd_20250310_162705.sh        ❌ 生成的脚本，应删除
├── config.json                              ⚠️  检查是否需要
├── diagnose_tokenizer.py                    ❌ 移动到 tools/diagnostics/
├── optuna_history_20250310_162704.png       ❌ 图片，应删除或归档
├── optuna_importance_20250310_162704.png    ❌ 图片，应删除或归档
├── optuna_results_20250309_202621.txt       ❌ 结果文件，应删除或归档
├── optuna_results_20250310_162705.txt       ❌ 结果文件，应删除或归档
├── run_apt.py                               ⚠️  检查是否还在使用
├── train.txt                                ❌ 文本文件，应删除
├── zh_train.txt                             ❌ 文本文件，应删除
└── 分层语言启蒙数据集.txt                   ❌ 文本文件，应删除或移到 docs/
```

---

## 目标架构

### 最终的理想结构

```
APT-Transformer/
│
├── apt/                          # 核心框架 (新架构)
│   ├── core/                     # L0 - 内核层
│   │   ├── modeling/             # 模型架构
│   │   ├── training/             # 训练逻辑
│   │   ├── data/                 # 数据处理
│   │   ├── generation/           # 生成引擎
│   │   ├── config/               # 配置管理
│   │   ├── runtime/              # 运行时
│   │   └── pretraining/          # 预训练
│   │
│   ├── perf/                     # L1 - 性能层
│   │   ├── providers/            # 性能提供者
│   │   └── optimization/         # 优化器
│   │
│   ├── memory/                   # L2 - 内存层
│   │   └── ...                   # 内存管理
│   │
│   ├── apps/                     # L3 - 应用层
│   │   ├── cli/                  # 命令行工具
│   │   ├── api/                  # API服务
│   │   ├── webui/                # Web界面
│   │   ├── console/              # 控制台
│   │   ├── evaluation/           # 评估工具
│   │   ├── interactive/          # 交互式工具
│   │   ├── agent/                # Agent功能
│   │   ├── rl/                   # 强化学习
│   │   ├── report/               # 报告生成
│   │   └── plugins/              # 插件系统
│   │
│   └── multilingual/             # 多语言支持
│
├── apt_model/                    # 工具和向后兼容层
│   ├── __init__.py               # 包入口
│   ├── __main__.py               # CLI入口
│   ├── main.py                   # 主函数
│   ├── utils/                    # 工具函数 (保留)
│   ├── codecs/                   # 编解码器 (可能保留)
│   ├── resources/                # 静态资源 (检查后决定)
│   ├── modeling/                 # 代理重导出 (向后兼容)
│   └── training/                 # 代理重导出 (向后兼容)
│
├── datasets/                     # 数据集 (重命名自 data/)
│   └── hlbd/                     # HLBD数据集
│
├── resources/                    # 资源文件
│   └── models/                   # 预训练模型
│       └── bert/                 # BERT模型 (从根目录移入)
│
├── examples/                     # 示例代码
│   ├── training_scripts/         # 训练脚本 (从 training/ 移入)
│   ├── profiles/                 # 配置示例 (从根目录移入)
│   ├── visualization/            # 可视化示例
│   └── ...                       # 其他示例
│
├── scripts/                      # 脚本工具
│   ├── testing/                  # 测试脚本
│   ├── setup/                    # 设置脚本
│   ├── maintenance/              # 维护脚本
│   └── ...
│
├── tools/                        # 开发工具
│   ├── diagnostics/              # 诊断工具
│   ├── data_generation/          # 数据生成
│   └── visualization/            # 可视化工具
│
├── docs/                         # 文档
├── tests/                        # 测试
├── archived/                     # 归档文件
├── experiments/                  # 实验配置
│
├── setup.py                      # 安装配置
├── requirements.txt              # 依赖
└── README.md                     # 说明文档
```

---

## 迁移计划

### 阶段1: 根目录清理 (低风险)

```bash
# 1. 移动 bert/ 到 resources/models/
mkdir -p resources/models
git mv bert resources/models/

# 2. 移动 training/ 到 examples/training_scripts/
git mv training examples/training_scripts

# 3. 重命名 data/ 为 datasets/
git mv data datasets

# 4. 移动 demo_visualization/ 到 examples/
git mv demo_visualization examples/visualization

# 5. 移动 profiles/ 到 examples/
git mv profiles examples/profiles

# 6. 删除临时文件
rm -f fix_torch_imports.py fix_issues.sh train.py
rm -f repo_index.json make_repo_index.py

# 7. 移动 test_logs/ 到 tests/
git mv test_logs tests/logs
```

### 阶段2: apt_model/ 清理 (中风险)

#### 2.1 删除临时文件

```bash
cd apt_model/
rm -f *.db *.bat *.sh
rm -f *.png *.txt
rm -f config.json
```

#### 2.2 迁移核心模块到 apt/core/

**需要检查的目录**:
- apt_model/core/ → apt/core/ (检查是否有遗漏文件)
- apt_model/config/ → apt/core/config/ (检查是否已完全迁移)
- apt_model/generation/ → apt/core/generation/ (检查是否已完全迁移)
- apt_model/data/ → apt/core/data/ (检查是否已完全迁移)
- apt_model/runtime/ → apt/core/runtime/ (检查是否已完全迁移)
- apt_model/pretraining/ → apt/core/pretraining/

#### 2.3 迁移应用层到 apt/apps/

**需要迁移的目录**:
- apt_model/agent/ → apt/apps/agent/
- apt_model/api/ → apt/apps/api/
- apt_model/cli/ → apt/apps/cli/
- apt_model/console/ → apt/apps/console/
- apt_model/evaluation/ → apt/apps/evaluation/
- apt_model/interactive/ → apt/apps/interactive/
- apt_model/report/ → apt/apps/report/
- apt_model/rl/ → apt/apps/rl/
- apt_model/tools/ → apt/apps/tools/
- apt_model/webui/ → apt/apps/webui/

#### 2.4 迁移其他层

- apt_model/memory/ → apt/memory/
- apt_model/optimization/ → apt/perf/optimization/

#### 2.5 保留的目录

- apt_model/utils/ (工具函数库)
- apt_model/codecs/ (编解码器，检查后决定)
- apt_model/resources/ (静态资源，检查后决定)
- apt_model/modeling/ (代理重导出)
- apt_model/training/ (代理重导出)

### 阶段3: 更新导入路径 (高风险)

所有迁移完成后，需要批量更新导入路径:

```python
# 旧路径
from apt_model.cli.xxx import YYY
from apt_model.api.xxx import YYY
from apt_model.evaluation.xxx import YYY

# 新路径
from apt.apps.cli.xxx import YYY
from apt.apps.api.xxx import YYY
from apt.apps.evaluation.xxx import YYY
```

### 阶段4: 创建向后兼容代理 (安全)

在 apt_model/ 下创建代理模块，重导出所有迁移的符号。

---

## 执行顺序建议

### 推荐执行顺序 (从低风险到高风险)

1. ✅ **先执行阶段1** - 根目录清理 (几乎无风险)
   - 这些都是示例、资源、临时文件
   - 不影响代码导入

2. ⚠️ **再执行阶段2.1** - 删除apt_model/临时文件 (低风险)
   - 只删除明确的临时文件
   - 不影响代码功能

3. ⚠️ **检查现有迁移状态** - 避免重复迁移
   - 对比 apt_model/core/ 和 apt/core/
   - 对比 apt_model/config/ 和 apt/core/config/
   - 确认哪些已迁移，哪些未迁移

4. 🚨 **谨慎执行阶段2.2-2.5** - 核心模块迁移 (高风险)
   - 需要逐个目录检查
   - 每次迁移后测试
   - 使用 git mv 保留历史

5. 🚨 **最后执行阶段3** - 更新导入路径 (最高风险)
   - 需要自动化脚本
   - 需要全面测试
   - 可能破坏现有功能

---

## 预期收益

### 清理后的效果

1. **根目录整洁**
   - 只保留必要的顶层目录
   - 所有示例在 examples/
   - 所有工具在 tools/ 或 scripts/

2. **apt/ 成为唯一核心**
   - 所有功能代码在 apt/
   - 清晰的L0/L1/L2/L3分层
   - apt_model/ 只保留工具和兼容层

3. **向后兼容**
   - 旧代码仍可正常工作
   - 通过代理重导出
   - 渐进式废弃警告

4. **可维护性提升**
   - 目录结构清晰
   - 功能划分明确
   - 易于新人理解

---

## 风险评估

### 高风险操作

- ❌ 移动 apt_model/cli/ (CLI是主入口)
- ❌ 移动 apt_model/core/ (可能有未迁移的核心代码)
- ❌ 批量更新导入路径 (可能遗漏或破坏导入)

### 中风险操作

- ⚠️ 移动 apt_model/api/, webui/ (可能有活跃使用)
- ⚠️ 移动 apt_model/evaluation/ (测试可能依赖)
- ⚠️ 移动应用层其他目录

### 低风险操作

- ✅ 根目录清理 (不影响导入)
- ✅ 删除临时文件 (可重新生成)
- ✅ 重命名/移动示例和资源

---

## 下一步行动

### 建议先做的事情

1. **先执行根目录清理** (阶段1)
   - 风险低
   - 立即改善项目整洁度
   - 不影响现有功能

2. **检查 apt_model/ 各目录状态**
   - 哪些已经在 apt/ 有对应目录
   - 哪些是完全独立的模块
   - 哪些可以安全迁移

3. **创建测试用例**
   - 确保迁移后功能不破坏
   - 特别是CLI命令测试
   - 导入路径测试

4. **逐步迁移，每次测试**
   - 一次迁移一个目录
   - 迁移后立即测试
   - 确认无问题再继续

### 用户确认

在执行任何操作前，请确认:

1. 是否同意这个清理计划？
2. 想从哪个阶段开始？
3. 对哪些目录有特殊要求？
4. 是否有任何目录不能动？

---

*创建时间: 2026-01-22*
*状态: 待用户确认*
